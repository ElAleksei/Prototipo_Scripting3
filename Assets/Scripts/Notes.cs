using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Notes : MonoBehaviour
{
    //Static para usar elementos con solo una instancia, como puntaje, hora, etc. public static (void) <name>. Se pueden acceder desde cualquier script
    //Lerp
    //IA: 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
                
    }
}
